package com.example.demo.model;

public enum MessageType {
    //We need to configure the message type for every time user loggin in,system confirms and while leaving the chat room
    CHAT,
    CONNECT,
    DISCONNECT
}
